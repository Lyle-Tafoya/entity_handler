require "spec_helper"

describe EntityHandler do
  it "has a version number" do
    expect(EntityHandler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
